import styled from "styled-components";
import colors from "../../config/colors";

const SortSelectButton = styled.select`
  border: transparent;
  background-color: ${colors.greyLight1};
  color: ${colors.greyDark2};
`;

export default SortSelectButton;
