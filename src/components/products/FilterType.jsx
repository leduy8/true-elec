import styled from "styled-components";
import colors from "../../config/colors";

const FilterType = styled.div`
  display: flex;
  padding-bottom: 2%;
  border-bottom: 1px solid ${colors.greyDark3};
  margin-bottom: 1%;
`;

export default FilterType;
