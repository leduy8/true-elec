import styled from "styled-components";

import colors from "./../../config/colors";

const SubSideNav = styled.ul`
  height: 0;
  overflow: hidden;
  color: ${colors.greyDark1};
  text-decoration: none;
  text-transform: uppercase;
  margin-left: 22%;
`;

export default SubSideNav;
